@model IEnumerable<TasksList.Models.Person>
@{
    ViewBag.Title = "Index";
}
<h2>
    Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
        </th>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Age
        </th>
        <th>
            Email Address
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
              @*  @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
                  @Html.ActionLink("Tasks", "TasksList", "", new { PersonId = item.Id }, "")
            </td>
            <td>
                @item.FirstName
            </td>
            <td>
                @item.LastName
            </td>
            <td>
                @item.Age
            </td>
            <td>
                @item.EmailAddress
            </td>
         
        </tr>
    }
</table>
